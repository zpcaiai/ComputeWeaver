# Generated contracts

Run `schema export schemas/json` and export `/openapi.json` from the API. Compatibility is
checked with `schema compatibility OLD NEW`. Persisted event contracts are versioned and are
never rewritten in place.
