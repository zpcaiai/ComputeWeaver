---
skill_id: B07
name: Energy Assets, PUE and Power-Balance Engine
version: 1.0.0
depends_on: B03,B06
status: NOT_STARTED
product: AI Compute-Energy-Infrastructure Control Plane
---

# B07 — Energy Assets, PUE and Power-Balance Engine

## 1. Mission

Implement grid, photovoltaic, battery, UPS, generator, cooling/PUE and facility power models with enforceable physical constraints.

## 2. Completion truth rule

Codex MUST NOT mark this batch `COMPLETE` because files were created or because code appears plausible. Completion requires all of the following:

1. Production implementation exists at the declared repository paths.
2. All required tests execute and pass.
3. Evidence artifacts are generated by the actual commands and reference the current commit.
4. No required interface is a stub, mock-only endpoint or unconditional success path.
5. The batch-specific Definition of Done and handoff contract are satisfied.

Allowed statuses are:

```text
NOT_STARTED
IMPLEMENTING
BLOCKED
EVIDENCE_PENDING
COMPLETE
```

## 3. Dependencies

`B03,B06`

Codex must verify dependency evidence before changing this batch to `IMPLEMENTING`. When a dependency interface is unavailable, create a typed temporary adapter behind the declared contract and keep the batch `BLOCKED` or `EVIDENCE_PENDING`; do not silently invent incompatible APIs.

## 4. Inputs

- Site topology and power zones.
- Tariff and carbon signals from B06.
- Battery capacity, efficiency, SOC, degradation and reserve requirements.
- Facility PUE, fixed loads, grid capacity and backup generation parameters.

## 5. Scope and required code modules

- `packages/energy/assets`: grid, PV, battery, UPS and generator models.
- `packages/energy/battery`: SOC transition, modes and degradation.
- `packages/energy/power_balance`: facility energy conservation and capacity constraints.
- `packages/energy/pue`: IT-to-facility power conversion.
- `packages/energy/state`: actual, forecast and planned values.
- `apps/api/energy` and energy asset UI.

## 6. Required interfaces

- `GET /v1/energy/state`, `POST /v1/energy/assets`, `POST /v1/energy/power-balance/validate`.
- Events: `EnergyStateObserved`, `BatterySocChanged`, `GridCapacityChanged`, `AssetDerated`.
- Power-balance service accepts compute load plus dispatch and returns residuals and violations.

All write interfaces must specify authentication, authorization, tenant/site scope, idempotency behavior, validation errors, audit event and rollback/compensation behavior where applicable.

## 7. Implementation workflow

1. Inspect the repository and dependency evidence; record the exact commit.
2. Produce or update an implementation plan mapping every required module to concrete files.
3. Implement domain models and pure logic before transport and UI code.
4. Implement APIs/events/adapters using B02 contracts.
5. Add migrations, seed fixtures and configuration documentation.
6. Add tests before marking interfaces complete.
7. Run the batch verification commands from a clean state.
8. Generate the evidence pack automatically.
9. Review open TODO, FIXME, skipped tests and mock-only paths.
10. Update the batch manifest and handoff notes.

## 8. Required tests

- Energy conservation across every interval.
- Battery SOC bounds, efficiencies and no simultaneous charge/discharge.
- Grid and site capacity enforcement.
- PUE and fixed-load calculations.
- Generator start delay, minimum run and fuel constraints.
- Numerical tolerance and unit consistency tests.

Minimum test classes:

```text
unit
property/algorithm
API or event contract
integration
negative/failure
security/authorization where relevant
scenario or regression
```

Skipped tests require an explicit waiver in `evidence/{b['id']}/waivers.json`; a waiver cannot cover a mandatory safety gate.

## 9. Required evidence

- `evidence/B07/power-balance-cases.json`.
- `evidence/B07/battery-property-tests.xml`.
- `evidence/B07/constraint-violation-examples.json`.
- `evidence/B07/numerical-tolerance.md`.
- Reference 24-hour energy profile.

Every evidence file must include or be accompanied by:

```json
{{
  "batch": "{b['id']}",
  "git_commit": "<sha>",
  "generated_at": "<UTC timestamp>",
  "command": "<exact command>",
  "result": "PASS|FAIL",
  "tool_versions": {{}}
}}
```

Screenshots alone are not proof of backend correctness.

## 10. Quality and safety gates

- No secrets, production credentials or real device write targets in fixtures.
- No silent fallback that changes a hard constraint into a soft preference.
- No LLM-generated value may bypass typed validation or the mathematical/safety layer.
- All externally visible errors use the shared error contract.
- State-changing operations are auditable.
- Data lineage and version provenance are preserved.
- New dependencies are locked, scanned and documented.
- Public interfaces receive contract tests.
- Failed mandatory gates block completion.

## 11. Definition of Done

- Power balance closes within documented numerical tolerance.
- Unsafe SOC, capacity and generator states are rejected.
- Actual, forecast and planned energy values are never conflated.
- Every energy asset is tied to a topology boundary.
- The engine exposes reusable constraints for B13.

## 12. Codex execution prompt

```text
Implement B07 exactly against this SKILL.md.

Before coding:
- inspect dependency implementations and evidence;
- list the concrete files you will create or modify;
- identify any unresolved interface mismatch.

During coding:
- implement real behavior, not specification-only files;
- add tests with each module;
- keep external writes disabled unless this batch explicitly enables guarded execution.

Before claiming completion:
- run all required commands from a clean state;
- generate evidence/B07;
- enumerate any skipped test, mock-only path, TODO or unsupported case;
- leave status EVIDENCE_PENDING if proof is incomplete.

Return:
1. changed files;
2. commands run;
3. test results;
4. evidence paths;
5. unresolved risks;
6. exact DoD checklist with PASS/FAIL for every item.
```

## 13. Handoff

B08 creates the data ingestion and quality layer feeding compute, energy and forecasting.
